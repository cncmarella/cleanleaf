# Copy to .env.local for development. Next.js loads .env.local automatically.
#
# NEXT_PUBLIC_ is required: the contact form posts from the browser, so this
# value is inlined into the client bundle at build time and is public. Never
# put a secret behind a NEXT_PUBLIC_ prefix.
NEXT_PUBLIC_API_URL=http://localhost:8080
